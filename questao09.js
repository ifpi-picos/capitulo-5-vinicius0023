function prossiga = true;

while (prossiga){
    let altura = Number(prompt("digite sua altura em metros: "))
    let peso = Number(prompt("digite seu peso em kilogramas: "))

if (altura > 0 && peso > 0){
    let imc = peso / (altura * altura)

    console.log ("IMC igual= "+ imc.toFixed(2))
    switch (true){

    }
}
} 